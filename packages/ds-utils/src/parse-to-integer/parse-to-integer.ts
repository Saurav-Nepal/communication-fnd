/**
 * @param  {string} number : String to be parsed to integer
 * @returns {number} : Returns the parsed integer
 */
export const parseToInteger = (number: string): number => {
    return parseInt(number, 10);
};
