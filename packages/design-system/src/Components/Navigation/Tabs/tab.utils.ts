/**
 * Checks if a given key is equal to the active key.
 * @param key The key to check.
 * @param active The currently active key.
 * @returns A boolean value indicating whether the given key is equal to the active key.
 */
export const isCurrentActive = (key, active) => key === active;
